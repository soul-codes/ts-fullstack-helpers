{
  "name": "@ts-fullstack-helpers/define-endpoint",
  "version": "0.2.0-alpha.2",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepare": "rm -rf dist && tsc",
    "watch": "tsc --watch --preserveWatchOutput"
  },
  "main": "dist",
  "dependencies": {
    "@ts-fullstack-helpers/schema": "^0.2.0-alpha.2",
    "typescript": "^3.1.6"
  }
}
