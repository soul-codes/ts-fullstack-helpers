{
  "name": "@ts-fullstack-helpers/define-endpoint",
  "version": "0.0.2-dev.3",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prepare": "rm -rf dist && tsc",
    "watch": "tsc --watch --preserveWatchOutput"
  },
  "main": "dist",
  "dependencies": {
    "@ts-fullstack-helpers/schema": "^0.0.2-dev.3",
    "typescript": "^3.1.6"
  }
}
